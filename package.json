{
  "name": "@celigo/ai-assets",
  "version": "0.1.0",
  "description": "Celigo AI building blocks (skills, MCP prompts & resources) consumed by internal tooling. Distributed to end users via skills.sh and the Claude Code plugin; this package.json exists so internal repos can vendor the assets as a pinned git dependency.",
  "private": true,
  "license": "MIT",
  "repository": "github:celigo/ai",
  "files": [
    "skills",
    "mcp"
  ],
  "scripts": {
    "sync-schemas": "node scripts/sync-schemas.mjs",
    "check-schemas": "node scripts/check-schemas-sync.mjs"
  }
}
