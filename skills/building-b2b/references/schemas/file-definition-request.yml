Request:
  type: object
  description: |
    Request schema for creating or updating a file definition.
  required:
    - name
    - version
    - format
    - rules
  allOf:
    - $ref: "./base.yml#/FileDefinitionBase"
